{
  "src/**/*.ts": [
    "eslint --max-warnings 0",
    "bash -c 'npx tsc --noEmit'"
  ],
  "src/**/*.{ts,test.ts}": [
    "vitest related --run"
  ],
  "backend/src/**/*.ts": [
    "bash -c 'bash \"$(git rev-parse --show-toplevel)/scripts/check-line-count.sh\" --staged'"
  ]
}
